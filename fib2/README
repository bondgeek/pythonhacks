Taken from the "Anatomy of Python C Module -- SuperJared.com

http://superjared.com/entry/anatomy-python-c-module/

Example illustrating the creation of Python module in C.

Implements a C version of the following Python code:

def fib2(n): # return Fibonacci series up to n
    """Return a list containing the Fibonacci series up to n."""
    result = []
    a, b = 0, 1
    while b < n:
        result.append(b)    # see below
        a, b = b, a+b
    return result

